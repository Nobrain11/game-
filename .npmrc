# NPM Configuration for better-sqlite3 compatibility
# This helps with installing native modules

legacy-peer-deps=true
ignore-scripts=false

# Fallback settings if build issues occur
# Uncomment the lines below if you encounter build problems:
# unsafe-perm=true
# build-from-source=false
